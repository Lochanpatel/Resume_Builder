import React from 'react'

export default function App() {
  return (
    <div style={{fontFamily: 'sans-serif', padding: 20}}>
      <h1>Resume Builder — Frontend Ready</h1>
      <p>Run <code>npm run dev</code> in the `frontend` folder to start.</p>
    </div>
  )
}
